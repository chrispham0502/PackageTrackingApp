[phases.setup]
providers = ['python']
nixPkgs = ['gcc', 'python38']
aptPkgs = ['libpq-dev', 'libgssapi_krb5']

[phases.install]
cmds = ['python -m venv /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt']

[start]
cmd = '. /opt/venv/bin/activate && gunicorn app:app'